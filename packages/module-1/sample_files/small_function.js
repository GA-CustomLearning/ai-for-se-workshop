function calculateArea(length, width) {
  // This function calculates the area of a rectangle.
  // It takes length and width as input and returns the area.
  return length * width;
}

// Example usage:
const area = calculateArea(10, 5);
console.log(`The area is: ${area}`);
